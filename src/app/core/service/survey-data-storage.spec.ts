import { SurveyDataStorage } from './survey-data-storage';

describe('SurveyDataStorage', () => {
  it('should create an instance', () => {
    expect(new SurveyDataStorage()).toBeTruthy();
  });
});
