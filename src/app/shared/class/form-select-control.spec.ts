import { FormSelectControl } from './form-select-control';

describe('FormSelectControl', () => {
  it('should create an instance', () => {
    expect(new FormSelectControl()).toBeTruthy();
  });
});
